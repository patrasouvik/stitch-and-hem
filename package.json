{
  "name": "stitch-and-hem",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site and booking form for an alterations business, on Cloudflare Workers.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
