{
  "name": "stitch-and-hem",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site and booking form for an alterations business, on Cloudflare Pages.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=stitch-and-hem"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
